@using MVC.Models;
@model List<emp>
@{
    ViewBag.Title = "GetAllEmps";
}

<h2>GetAllEmps</h2>

<table>
    @foreach(emp em in Model)
    {
        <tr>
            <td>@em.name</td>
            <td>@em.salary</td>
        </tr>
    }
</table>